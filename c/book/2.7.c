#include "headers.h"

unsigned	invert(unsigned x, unsigned p, unsigned n)
{

	return (0);
}

int			main()
{
	unsigned	x	= 0;
	unsigned	p	= 4;
	unsigned	n	= 3;
	PrintToBaseN(invert(x, p, n), 2);
	return (0);
}
