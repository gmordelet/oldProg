#include <stdio.h>

void		PrintToBase(unsigned, unsigned);

unsigned	setbits(unsigned x, int p, int n, unsigned y)
{
	unsigned	ny	= ~(~0 << n) & y;
	unsigned	nx	= (~(~0 << p)) << 
	return ();
}

int main()
{
	
	return (0);
}
	
